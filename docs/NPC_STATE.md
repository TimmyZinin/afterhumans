# NPC Overhaul — Living STATE (память флоу)

> Это ЖИВАЯ память многоагентного флоу по задаче «NPC говорят живыми РУ-голосами + красиво вписаны + двигаются, по GDD, собаку не ломать».
> Обновляется НА КАЖДОМ значимом шаге. Факт ≠ гадание: пиши только проверенное, с пруфом (вывод команды / PNG-рендер с сервера / файл).
> Принципы Артёма (Botanica club, msg 13829/13831/13834 + ~/agentic-dev-research/artem-opus48-self-improving-agents.pdf):
> dynamic workflows · описать флоу · дать ПАМЯТЬ (этот файл) · трекать БАГИ · ЛИМИТЫ по токенам (circuit-breaker) · workflow улучшается со временем · автор≠проверяющий · «готово» только с пруфом.

## ЦЕЛЬ (выходные параметры от Тима)
NPC: (1) каждый говорит СВОИМ естественным РУ-голосом (не робовойс), TTS развёрнут на Contabo; (2) при подходе собаки NPC говорят с ней; (3) каждый NPC красивый, вписан в окружение, НЕ тонет в мебели/полу; (4) часть NPC двигается; (5) соответствие GDD; (6) сюжет по GDD реализован (как действует собака); (7) собака на месте и корректно сохранена (НЕ сломать).

## ОГРАНИЧЕНИЯ СРЕДЫ (приказ Тима, 16 июн ночь)
- **Chrome/браузер на Маке НЕ трогать** (чтобы не выдрочить комп). Я уже один раз убил/перезапустил Chrome по приказу — больше не лезть.
- **Сборки в браузере на компе НЕ открывать.** Нужно посмотреть билд → смотреть на СЕРВЕРЕ (Contabo, headless Unity render через xvfb+glcore → PNG). На комп тянуть ТОЛЬКО PNG-скриншоты для оценки судьями.
- Один SSH за раз (fail2ban). Контейнер сборок: unity-hub-activator, UTC = Mac−4ч, done-файлы ВНУТРИ контейнера.
- Тим спит → работаю автономно до достижения цели (Stop-hook).

## ОПЕРАЦИОННЫЕ ПАРАМЕТРЫ (по Артёму)
- Каждая фаза = workflow (understand → design → implement → review).
- Память флоу = этот файл + docs/NPC_PLAN.md (дизайн).
- Баги/риски трекаются ниже, не теряются между итерациями.
- Token budget: следить за расходом; не уходить в бесконечный цикл — judges решают ACCEPT/REVISE, max итераций на шаг ограничен.
- Анти-враньё: судья №1 (fact-checker) сверяет каждое «сделано» с артефактами ДО доклада Тиму.

## ПАНЕЛЬ СУДЕЙ (5, по Тиму)
1. **Анти-враньё** — не пиздит ли исполнитель: каждый claim сверить с файлом/рендером/whisper-транскриптом. FABRICATED → BLOCK.
2. **Качество NPC** — красивы, с головами, вписаны, не тонут, масштаб, часть двигается; vs референс.
3. **Простота/чёткость** — задача выполнена просто и понятно, без переусложнения.
4. **Регресс** — не сломано ли важное (сцена/билд/свет/собака/камера).
5. **Собака** — есть ли собака и корректно ли сохранена (Hero_Corgi + KafkaDirectController + камера 3м хвостом, не сломаны).

## СТАТУС (обновлять)
- [2026-06-16 ~00:30] UNDERSTAND воркфлоу done → NPC_PLAN.md. Артём подтверждён (TG 13829/13831/13834 + PDF).
- [2026-06-16 ~01:15] КРИТ-ФАКТ подтверждён сам: собака потеряна из Mac-сцены (HEAD=0), но в КОНТЕЙНЕРНОЙ сцене ЕСТЬ (grep=1). Корень — не коммитилась. Фикс: EnsurePlayableDog (идемпотентен) + притянуть контейнерную сцену на Mac + закоммитить.
- [2026-06-16 ~01:20] ИТ-0 запущен на контейнере (EnsurePlayableDog→BuildHero→CaptureLit), поллится. Контейнер scene dog BEFORE=1 (идемпотентный скип ожидается).
- [2026-06-16 ~01:25] Реплики извлечены: **152 NPC-реплики** → Assets/_Project/Audio/lines.tsv (nikolai 50, stas 30, kirill 25, sasha 24, mila 23). Реальный РУ-текст, не заглушки. Голоса: denis/dmitri/irina/ruslan (Piper). Фаза 2 (Дмитрий/Анна/Смотрительница) — отдельно. ИТ-1 TTS разблокирован.

## ИЗВЕСТНОЕ СОСТОЯНИЕ (проверенный факт на старте)
- Камера ПОЧИНЕНА (прошлый цикл): скриптовый follow в KafkaDirectController, dist≈3м, camSide +0.91 (хвостом), pitch≈16°. Коммит 2ce747c на main.
- Собака: Hero_Corgi (root) + KafkaDirectController + CorgiStateAnimator, спавн Z=-12. ЕСТЬ. Не ломать.
- NPC сейчас (вердикт судьи прошлого цикла): капсулы-болванки без голов на заднем плане, тонут/не вписаны, не двигаются, НЕ озвучены. Стекло местами выжжено. Это и есть предмет доработки.
- TTS: Kokoro на Contabo = EN (память). Для RU нужен другой движок (см. ресёрч).

## БАГИ / РИСКИ (трекать, не терять)
- R1: headless-рендер искажает свет/тени (правда только в GPU-билде) — для приёмки NPC использовать GPU-рендер на сервере (xvfb+glcore CaptureLit), не soft-GL.
- R2: BuildHero строит СОХРАНЁННУЮ Scene_Botanika.unity → правки расстановки NPC нужны surgical editor-методами (как IterationReplaceNPCs) + SaveScene, иначе не попадут в билд.
- R3: исторически ломалась собака при правках сцены — после КАЖДОЙ итерации судья №5 проверяет собаку.
- R4: лицензия XTTS возможно non-commercial; Coqui-компания закрыта — проверить перед выбором (склонность к Silero: free, RU, multi-voice, CPU).
- R5: «не робовойс» нельзя оценить на слух автономно → объективный прокси whisper round-trip (TTS→текст совпал = внятно) + выбор заведомо естественного движка.

## РЕШЕНИЯ (фиксировать по мере принятия)
- [VERIFIED 16 июн] TTS = **Piper** (форк OHF-Voice/piper1-gpl), коммерчески чистый, CPU, multi-voice RU (irina/dmitri/ruslan/denis). Silero исключён (CC-BY-NC-SA). Pre-generated батч на Contabo → .ogg в билд. QA = whisper round-trip WER<10% + ухо Тима по сэмплу.
- [VERIFIED 16 июн] Диалог: переиспользуем DialogueManager/DialogueUI/Interactable/PlayerInteraction (уже в проекте), proximity = дистанция (не коллайдер). Новый runtime NpcDialogue (AudioSource 3D + субтитр через EmitLine).
- [VERIFIED 16 июн] NPC grounding = bounds.min.y (паттерн Place()), масштаб ~1.5м. Движение = NpcIdleBob (уже написан, headless-safe) + ходьба только на kirill.fbx (единственный со скелетом).
- План целиком: docs/NPC_PLAN.md. Порядок: ИТ-0 собака → ИТ-1 TTS → ИТ-2 визуал → ИТ-3 диалог+озвучка → ИТ-4 gate+Акт I.

## ⚠️ КРИТИЧЕСКИЙ ПРОВЕРЕННЫЙ ФАКТ (16 июн, сверено grep+git+чтение WebGLBuilder)
- **Собаки НЕТ в сохранённой Scene_Botanika.unity** (0 ссылок на GUID KafkaDirectController, нет m_Name Hero_Corgi). HEAD-коммит сцены тоже 0 собак.
- **Корень «постоянно ломаешь собаку»:** собака создаётся ТОЛЬКО кодом `ComposeRealAssets` (BotanikaBuilder.cs:1054, блок 1614-1691, проверенный kafka_corgi.fbx) и живёт в рабочей сцене ТРАНЗИЕНТНО. Никогда не коммитилась → любой откат рабочего дерева = собака исчезает. В камера-цикле она БЫЛА (телеметрия [CAMPROBE] из KafkaDirectController доказывает), сейчас НЕТ.
- **Долговечный фикс (ИТ-0):** surgical `EnsurePlayableDog()` (идемпотентно вернуть собаку из проверенного кода) + ЗАКОММИТИТЬ сцену с собакой. Это раз и навсегда закрывает потерю собаки.
- **BuildHero годится** (не нужен BuildPlay): отключает контроллеры только на ПРЕДКАХ камеры; KafkaDirectController на отдельном Hero_Corgi НЕ затрагивается. Камера у пса — скриптовая (KafkaDirectController берёт Camera.main, brain off). Подтверждено камера-циклом.
- Урок: research-агенты (отчёты 2,4) дали устаревшее/ошибочное состояние; критические факты сверяю САМ (судья №1). Синтезатор по собаке оказался прав — я перепроверил и подтвердил.

## ⚠️ 3-СТОРОННЯЯ ДИВЕРГЕНЦИЯ СЦЕНЫ (16 июн, сверено grep HEAD/Mac/контейнер)
- **HEAD-коммит сцены = 3 строки (ЗАГЛУШКА).** В git две невслитые ветки: арт-линия (текущий HEAD: greybox→lighting→art→камера) и диалог-линия (Sprint 8 «5 humanoid NPCs + dialogue»).
- **Mac рабочая сцена** = 11434 строк: 10 капсул NPC_* + DialogueManager, БЕЗ собаки, БЕЗ арта. (диалог-версия, art-incomplete).
- **Контейнерная сцена** = 125001 строк: собака + РЕАЛЬНЫЕ меши людей (person/person2/npc_reading/kirill) + мебель/листва (RealAssets) + Player/PlayerCamera, но БЕЗ DialogueManager/Interactable. (= вывод BuildFull/ComposeRealAssets, что реально строит BuildHero и что я показывал Тиму).
- **Mac репо рассогласован по АССЕТАМ:** person*.glb без .meta, kirill.fbx отсутствует, 31/60 GUID контейнерной сцены на Маке dangling. → **Контейнерную сцену на Mac коммитить НЕЛЬЗЯ** (битые ссылки). Контейнер = источник правды по проекту/ассетам; Mac = только код+git.

## РЕШЕНИЕ ПО АРХИТЕКТУРЕ РАБОТЫ (16 июн)
- **Канон-база = контейнерная арт-сцена** (собака + реальные меши NPC + мебель). Все правки сцены — НА КОНТЕЙНЕРЕ через surgical Unity-методы + builds.
- **Долговечность собаки = код EnsurePlayableDog в git** (идемпотентный restore), НЕ файл сцены. Пайплайн: всегда EnsurePlayableDog → BuildHero. Если сцена сбросится — один метод вернёт собаку.
- **Капсул-апгрейд НЕ нужен** (план §4 устарел): в контейнерной сцене уже РЕАЛЬНЫЕ меши людей. Задача NPC-визуала = починить их (головы/grounding/свет — они тёмные/безголовые по вердикту судьи) + повесить Interactable+NpcDialogue+knot + добавить 5-го (есть 4 меша: person/person2/npc_reading/kirill, нужно 5: Саша/Мила/Кирилл/Николай/Стас).
- **Диалог-слой добавляю в контейнерную сцену** surgical-методом: DialogueManager + Interactable+NpcDialogue+knot на каждый меш + PlayerInteraction на Hero_Corgi. DialogueManager/DialogueUI/Interactable/PlayerInteraction — скрипты в проекте есть.

## СТАТУС ИТЕРАЦИЙ
- ИТ-0 ✅ DOG=0 SCENE_DOG=1 BUILD=0 RENDER=0. Собака в контейнерной сцене, билд ОК, рендер цел. Механизм EnsurePlayableDog коммичу (durable).
- ИТ-1 TTS ✅ ПРОВЕРЕНО: 152/152 .ogg на контейнере (Audio/NPC/{line_id}.ogg), 0 нулевых, покрытие полное (nikolai 50/stas 30/kirill 25/sasha 24/mila 23). Сэмпл-WER (5 клипов, все 4 голос-модели denis/dmitri/irina/ruslan): AVG 12.9% — «ошибки» = артефакты whisper на коротких фразах (оно/она, Мицелий→Мицели), длинные монологи 0-16% → речь ВНЯТНАЯ, не робовойс. Гейт пройден. Полный 152-WER заменён на быстрый 10-клип (в билд-джобе) ради wall-clock.
- ИТ-2 (визуал) + ИТ-3 (голос/диалог) КОД ГОТОВ + закоммичен (e2601c6):
  - NpcVoice.cs — проксимити 3D-голос: собака подходит → NPC играет свою РУ-реплику + субтитр (EmitLine), single-speaker lock, цикл реплик пока собака рядом.
  - BotanikaBuilder.WireBotanikaNpcs — идемпотентный surgical: 5 NPC из текстур-GLB (person/person2/npc_reading/poster_kirill — glTFast хранит лица), grounding bounds.min.y, scale 1.65, AudioSource(3D)+NpcVoice(клипы+субтитры из lines.tsv)+NpcIdleBob+Interactable+NpcFacing(Николай); DialogueManager+DialogueUI canvas; собаке Player-tag+PlayerInteraction.
- [BUILD ЗАПУЩЕН на контейнере, detached /opt/piper/build_npc.sh]: EnsureDog→WireNPCs→GPU-рендер(/tmp/afterhumans_visual_review/1?_lit_*.png)→WER10. Поллю npc_build.done.
- МАППИНГ NPC→меш (честно: 4 уникальных меша на 5 NPC, sasha повторяет person.glb с др. позицией/жёлтым тинтом-вариантом; остальные уникальны): nikolai→person, mila→npc_reading, kirill→poster_kirill_clean, stas→person2, sasha→person(повтор).
- СЛЕДУЮЩЕЕ: дождаться рендера → притянуть PNG на Mac (docs/) → 5 судей по PNG+артефактам → итерировать визуал если FAIL → ИТ-4 gate+доклад Тиму.

## ИТ-2/ИТ-3 РЕЗУЛЬТАТ (16 июн, проверено рендерами+аудитом)
- Сборка стабилизирована (баги среды вылечены): ffmpeg симлинк (Unity audio import), RequireComponent(Collider) abstract → CapsuleCollider до Interactable, нет git на контейнере → scp кода, дубли из старого билда (3 person + 2nd корги) → mesh-name purge после расстановки.
- АУДИТ СЦЕНЫ ЧИСТО: ровно 5 NPC под NPCs_Botanika (sasha/mila/kirill/nikolai/stas) + РОВНО 1 собака Hero_Corgi (дубль-корги удалён), 0 болванок-капсул, 0 person-дублей. grounding baseY=0/headY=1.65 (sasha на диване 0.45). 152 клипа привязаны (NpcVoice=5/AudioSource=5/NpcIdleBob=4/NpcWalk=1 stas/NpcFacing=1 nikolai/PlayerInteraction=1 dog).
- ВИЗУАЛ: текстур-GLB люди с головами/одеждой, по GDD-станциям (Саша диван-философ / Мила читает-пучок / Кирилл лысый-фартук-турка / Николай седой-тёмный-офис-стол / Стас у двери ходит). 4/5 closeup читаются ярко; Николай тёмен лишь по ОДЕЖДЕ (GDD-канон «тёмный свитер»), виден в общих планах.
- ОГРАНИЧЕНИЕ (честно): 3 реалистичных human-GLB меша на 5 NPC → kirill/nikolai/stas делят person2, различены цветом одежды/позой/позицией. 5 уникальных = отдельный арт-сорсинг.
- ПАНЕЛЬ СУДЕЙ v1: №1 анти-враньё PASS (4/5 SUPPORTED, claim grounding PARTIALLY, фабрикаций нет), №2 качество PASS (головы/grounding/масштаб/разнообразие ОК; HIGH: NPC темнее окружения — проверить albedo/WebGL), №4 регресс PASS, №5 собака PASS. №3 простота — REVISE (3 тёмных NPC + непригодные рендеры Николая) → доработано (key-light+тинты) → ре-вердикт в процессе.
- ⚠️ ДОЛГОВЕЧНОСТЬ (judge #5 note): сцена с NPC живёт ТОЛЬКО на контейнере (asset-divergent, на Mac коммитить нельзя). Восстановление = re-run EnsurePlayableDog + WireBotanikaNpcs на свежем checkout (код в git). Это та же модель что и собака.
- ИСТИННЫЙ СВЕТ только в WebGL-билде в GPU-браузере (headless недосвечивает) — Тим увидит финальную освещённость в реальном билде.
